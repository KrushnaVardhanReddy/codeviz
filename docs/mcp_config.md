# MCP Client Configurations

## Claude Desktop
```json
// ~/.config/claude/claude_desktop_config.json
{
  "mcpServers": {
    "codeviz": {
      "command": "codeviz",
      "args": ["serve", "--mcp"]
    }
  }
}
```

## Cursor
```json
// .cursor/mcp.json
{
  "mcpServers": {
    "codeviz": {
      "command": "codeviz",
      "args": ["serve", "--mcp"]
    }
  }
}
```

## Continue.dev
```json
// .continue/config.json
{
  "mcpServers": {
    "codeviz": {
      "command": "codeviz",
      "args": ["serve", "--mcp"]
    }
  }
}
```
